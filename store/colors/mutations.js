export default {
    changeRecColors (state, recColors) {
        state.recColors = recColors
    },

    changeAllColors (state, allColors) {
        state.allColors = allColors
    },
    
    changePrimaryColor (state, color) {
        state.primaryColor = color
    }
}